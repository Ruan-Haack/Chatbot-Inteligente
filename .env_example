# Segurança (Crie uma chave longa e aleatória)
SECRET_KEY=sua_chave_secreta_aqui_muito_longa_123

# Banco de Dados (Usaremos SQLite para desenvolvimento local)
DATABASE_URL=sqlite:///./dev.db

# Tempo de expiração do Token JWT (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Se for usar a API da OpenAI (adicione sua chave se tiver)
OPENAI_API_KEY=sk-seu_token_aqui

# Configurações de Ambiente
APP_ENV=dev
LOG_LEVEL=info